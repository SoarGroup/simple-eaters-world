sp {hierarchical-SMEM*elaborate*state
   (state <s> ^superstate.operator <o>)
   (<o> ^name <name>)
-->
   (<s> ^name <name>)
}

sp {hierarchical-SMEM*elaborate*state*io
   (state <s> ^superstate.io <io>)
-->
   (<s> ^io <io>)
}