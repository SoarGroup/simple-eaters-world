sp {hierarchical-RL*move*propose*forward
   (state <s> ^name move
              ^move-direction <dir>
              ^io.input-link.orientation <dir>)
-->
   (<s> ^operator <op> + =)
   (<op> ^name forward)
}

sp {hierarchical-RL*move*apply*forward
   (state <s> ^operator <op>
              ^io.output-link <ol>)
   (<op> ^name forward)
-->
   (<ol> ^forward <f>)
}