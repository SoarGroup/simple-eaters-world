sp {hierarchical-rl*elaborate*state
   (state <s> ^superstate.operator <o>)
   (<o> ^name <name>)
-->
   (<s> ^name <name>)
}

sp {hierarchical-rl*elaborate*state*io
   (state <s> ^superstate.io <io>)
-->
   (<s> ^io <io>)
}