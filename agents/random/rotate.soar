sp {random*propose*rotate
   (state <s> ^name random
              ^io.input-link.time)
-->
   (<s> ^operator <op> + =)
   (<op> ^name rotate)
}

sp {apply*rotate
   (state <s> ^operator <op>
              ^io.output-link <out>)
   (<op> ^name rotate)
-->
   (<out> ^rotate <r>)
}
